# 2026-05-30

## [03:50] query | design-review — 現時点の設計見直し・改善点の洗い出し

`wiki/outputs/queries/2026-05-30-design-review.md` に保存。
2026-05-13 の design-issues 分析との差分を取り、現況をコード直接調査で検証:
- **改善済**: SpreadsheetGrid 1154→400行（フック/サブコンポーネントに分割）、columnTypeConfig でメタデータ集約
- **残課題**: ColumnType の描画/変換分岐（Cell 7・PageView 8・validator 3）、ViewQuery 型分岐の分散（新発見の第2軸）、project.store 766行（増加傾向）
- **要修正**: db-server PATCH の deletedIds 非対応（Gotchas #7）
優先度最上位は ColumnType の formatValue/coerce 関数集約。viewTypeConfig 新設を中優先で提案。

## [04:25] promote | design-review — ①② の実装完了を反映

優先度高①・中②を実装し main にマージ（PR #63 / #64）したため、design-review クエリと CLAUDE.md に状態を反映:
- ① ColumnType 表示整形 → `src/lib/formatCellValue.ts` に集約（Cell/PageView の重複解消）。当初の `coerce` 集約案は見送り（validator.ts に集約済のため）
- ② ViewQuery アイコン → `src/lib/viewTypeConfig.ts` に集約（EditorPage/SpreadsheetView の重複解消）
- 調査中に④ EditorPage ダイアログ管理が `useDialogState` で解消済と確認
- 優先度マトリクスに「状態」列を追加。残課題: ③ 編集ウィジェット JSX レジストリ化・project.store 責務分離・db-server PATCH 削除非対応

## [04:40] query | view-unification — ビュー型（filter/union/lookup）統合の調査

`wiki/outputs/queries/2026-05-30-view-unification.md` に保存。`raw/articles/view-architecture-idea.md` の着想を起点に調査:
- filter+lookup は単一 `SelectQuery`（from+join+where+select）に自然統合、union は SelectQuery の縦結合ラッパ → 3型が実質1.5種に
- 核心は読み取り変換でなく**編集の書き戻し（provenance）**。`_source`/`_sources` を `__origin` に一般化するのが設計の中心
- 保存形式は**構造化 JSON モデルを正本**にし、SQL/独自テキストは後付け入力層に留めるのが安全（「自由度が高すぎる」懸念の解消）
- 新インターフェース設計+>10ファイルで AGENTS.md の大規模タスク停止基準に該当 → Phase 0(設計確定)〜Phase 3(UI/DSL) の段階分割を提案

## [05:45] compile | view-unification 実装に伴う wiki 更新

ビュークエリ統合 Phase 1（PR #65）・Phase 2（PR #67）を main にマージ。実装と食い違っていた wiki を更新:
- `data-model/View_Queries`: 旧 filter/union/lookup → `SelectQuery`（joins 有無）/`UnionQuery`/`PageViewQuery`・`Join`・`_origin` に全面改稿
- `architecture/Domain_Logic`: ファイル表から lookup.ts 削除・select.ts 追加、`applySelect`/`applyUnion` と `_origin` 解決を記述
- `Gotchas` #8: 予約フィールド `_source`/`_sources` → `_origin = {table,id}` に更新
- `how-to/Add_View_Type`: ViewQuery 共用体を新型に、Step3 の計算箇所を project.store→EditorPage に訂正、`_origin` 記述に更新

## [05:55] lint | 現状コードと著しく違う wiki 箇所の修正（9 ファイル）

wiki 全体を grep でコード突き合わせし、旧ビュー型・削除済みファイル・旧予約フィールドを参照していた箇所を現状に修正:
- index.md / data-model/index.md / Project_Format.md / View_Dialogs.md: 旧 FilterViewQuery/UnionViewQuery/LookupViewQuery → SelectQuery/UnionQuery/PageViewQuery
- summaries/doc-architecture: union.ts/lookup.ts → select.ts/union.ts
- summaries/src-domain: lookup.ts 節を削除し select.ts 節に置換、union を _origin/UnionQuery に
- summaries/src-types: view.ts 型・row.ts 予約フィールド（_source/_sources→_origin）を更新
- summaries/src-spreadsheet: 削除時のソース特定を _origin/getRowOwnerTable に
- 据え置き: agent-patterns/Context_and_Cost の「SpreadsheetGrid 1154行」は過去 issue(LIN-171) の史実のため保持

## [12:30] compile | 未配線 SQLite バックエンド削除に伴う wiki 更新

design-review の「db-server PATCH 削除非対応」を調査した結果、DbServerAdapter が `setAdapter` から到達不能な死蔵コードと判明 → コード（`server/db-server.ts`/`src/fs/db-adapter.ts`・better-sqlite3 依存）を削除（別 PR）。これに伴い wiki を更新:
- summaries/server: 2 実装 → 1 実装（ファイルベース）に改稿、db-server スキーマ・deletedIds 注意を削除
- summaries/src-fs-adapters・FileSystem_Adapters: 3 実装 → 2 実装、DbServerAdapter を mermaid/節から削除
- Gotchas: #7（db-server PATCH 差異）を削除し #8（予約フィールド）を #7 に繰り上げ。参照 4 箇所も #7 に更新
- Dev_Workflow（db-server コマンド行）・Add_View_Type（Step6）・raw/refs（server/src-fs-adapters）を更新
- design-review クエリ・CLAUDE.md: 当該項目を「解決（削除）」に更新
