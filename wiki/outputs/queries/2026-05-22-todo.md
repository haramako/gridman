# Query: todo を調べて

`2026-05-22` に実行された、Gridman プロジェクトの wiki および開発計画における `todo`（オープンな課題・未完了タスク）の調査結果です。

---

## 🔖 調査概要

Gridman プロジェクトの `plan/mvp.md`、`wiki/wiki/index.md`、および `wiki/CLAUDE.md` から、現在残されている未完了のタスクおよびオープンな課題（TODO）を抽出・整理しました。

---

## 1. 開発計画における未完了タスク（`plan/mvp.md`）

開発計画書である [[plan/mvp]] では、Phase 3 および Phase 4 に未完了のタスクが残されています。

### 🔹 Phase 3: ルックアップ編集・ページビュー（ほぼ完了）
- [ ] **Undo / Redo の `updateCell` 統合**
  - インフラおよび `Ctrl+Z` / `Ctrl+Y` のショートカットキー自体は実装済みですが、`updateCell` への完全な統合が完了していません。

### 🔹 Phase 4: Web対応・拡張（未着手）
- [ ] **File System Access API 対応**
  - ローカルサーバーなしで、ChromeやEdgeブラウザから直接ローカルファイルシステムにアクセスできるようにする対応。
- [ ] **スキーマ編集 UI**
  - テーブルスキーマを GUI 上で直接編集できるようにする UI の構築。
- [ ] **ファイルエクスポート（JSON / CSV）**
  - 編集したデータを JSON や CSV 形式でダウンロード・エクスポートする機能。
- [ ] **列の表示 / 非表示切り替え**
  - スプレッドシート上でのカラムの表示・非表示を制御する機能。
- [ ] **DB バックエンドアダプター**
  - 将来的な拡張性を見据えた、データベース接続用のバックエンドアダプターの実装。

---

## 2. llm-wiki に記録されているオープンな課題（`CLAUDE.md` / `index.md`）

[[wiki/index]] および [[CLAUDE.md]] には、開発プロセスやドキュメントに関する以下のオープンな調査課題が挙げられています。

### 📝 ドキュメントの不足
- **`syncDraftFromTab` の動作詳細（マルチタブ同期）**
  - 複数タブで開いた場合のドラフト同期アルゴリズムの動作詳細が未ドキュメントのままとなっています。

### 🤖 エージェントパターンの改善と対策
- **Q4: `context-overload`（コンテキスト肥大化）への対策**
  - `countermeasure: none` となっている context-overload 系 issue（LIN-18, LIN-39, LIN-44, LIN-47, LIN-62, LIN-79, LIN-191, LIN-193, LIN-194, LIN-195, LIN-196, LIN-197, LIN-198 など計13件）への対策。
  - 「実装前にファイル数を確認する」といった指示を issue テンプレートへ追加することの検討が必要です。
- **Q5: PR 作成忘れパターン（`pr-skip`）の機械的チェック**
  - PR の作成忘れ（LIN-172, LIN-175, LIN-184 など）を防ぐため、`AGENTS.md` のルール記述だけでなく、CI/CD やスクリプトなどによる機械的なチェック方法の構築が課題となっています。
- **Q6: `quota-recovery`（不要な ping）の抑制**
  - `quota-recovery` が16件に増加しており、特に lint 修正タスク後の不要な ping が大幅に増えています。これらを抑制する具体的な手法の検討が必要です。

---

## 📅 次のアクション

1. **Phase 3 の完了**: `updateCell` と Undo / Redo の統合を優先して進める。
2. **`syncDraftFromTab` のドキュメント化**: `wiki/concepts/Auto_Save_and_Draft.md` に追記。
3. **エージェントのコンテキスト制限対策**: 実装前ファイル数チェックの仕組みをプロンプトや `AGENTS.md` 等に導入する。
