```json
{
  "identifier": "LIN-194",
  "title": "noExplicitAny の型解消",
  "status": "done",
  "run_count": 8,
  "total_tokens": 3179346,
  "has_real_failures": true,
  "failure_cause": "opencode タイムアウト（2回）とAPIオーバーロードエラー（1回）の計3回失敗。並行issueの大量起動と一時的なAPIサービス障害が重なった。",
  "rerun_causes": ["quota-recovery", "spec-feature-addition"],
  "patterns": ["quota-recovery", "spec-feature-addition"],
  "countermeasure": "one-time",
  "written_at": "2026-05-16"
}
```

# LIN-194: noExplicitAny の型解消

## 実行履歴サマリー

| Run | Status | トリガー |
|-----|--------|---------|
| 1 | failed | これらの対応方法をプランしてください（タイムアウト 2h） |
| 2 | failed | これらの対応方法をプランしてください（タイムアウト 2h） |
| 3 | failed | (direct — APIオーバーロード 529エラー) |
| 4 | completed | ping（プラン提示） |
| 5 | completed | OK, 実装をしてください |
| 6 | completed | ping（bare ping、作業済みのため何もせず） |
| 7 | completed | ping（bare ping、作業済みのため何もせず） |
| 8 | completed | ping — noExplicitAny修正がPR #60で完了、ステータスをin_reviewに更新してください |

## 観察

### タイムアウト2連発 + APIオーバーロード

Run 1・2 は "opencode timed out after 2h0m0s" で失敗（trigger: "これらの対応方法をプランしてください。まだ実装はしない"）。Run 3 は API Error 529 Overloaded で失敗。3連続失敗という最も困難なパターン。

### ping で段階的再開

Run 4 の "ping" でプランのみ提示（5箇所の `any` に対する具体的な修正案）。Run 5 の "OK, 実装をしてください" で実装を進め PR #60 を作成。

### bare ping の連鎖

Run 6・7 は bare ping に対して何もせず沈黙。Run 8 は kanade-opencode からの具体的な依頼 ping でステータスを `in_review` に更新。

### 最多 run 数（8回）

LIN-191〜198 の中で最多の8 run。3連続失敗と複数の ping 対応が重なった。

## 教訓

1. **タイムアウト案件はプランと実装を分割すると良い** — Run 1・2 は "プランしてください（実装はまだ）" がタイムアウトした。プランに2時間かかる場合はissue説明に具体的なファイル・行番号を含めることで処理時間を短縮できる。
2. **APIオーバーロードは一時的な環境問題** — Run 3 の 529 エラーは再試行で回復する。ただし連続失敗後は ping または新規コメントで手動再起動が必要。
3. **bare ping の連鎖を防ぐ** — kanade-opencode が複数回 ping を送り、エージェントが何もしない run が発生した。ping はステータス確認や具体的な依頼を含む形にすべき。
