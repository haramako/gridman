```json
{
  "identifier": "LIN-197",
  "title": "機械的リファクタリング (isNaN/forEach/テンプレートリテラル/delete/else)",
  "status": "done",
  "run_count": 5,
  "total_tokens": 2067080,
  "has_real_failures": true,
  "failure_cause": "opencode が2時間のタイムアウトで失敗（2回）。PR承認後のマージ処理中にタイムアウトし、マージが宙に浮いた状態になった。",
  "rerun_causes": ["quota-recovery"],
  "patterns": ["quota-recovery"],
  "countermeasure": "one-time",
  "written_at": "2026-05-16"
}
```

# LIN-197: 機械的リファクタリング (isNaN/forEach/テンプレートリテラル/delete/else)

## 実行履歴サマリー

| Run | Status | トリガー |
|-----|--------|---------|
| 1 | completed | (初回 direct — トリガーなし) |
| 2 | failed | PR確認OK（タイムアウト 2h） |
| 3 | failed | PR確認OK（retry attempt 2 — タイムアウト 2h） |
| 4 | completed | (direct — トリガーなし) |
| 5 | completed | PR確認OK |

## 観察

### 初回実装は成功

Run 1 で biome --unsafe による自動修正と手動 `noForEach` 修正を行い、PR #52 を作成。158件のテスト全パスを確認。正常完了。

### PR承認後のマージ処理でタイムアウト2連発

Run 2・3 は "PR確認OK" トリガーで起動したが、ともにタイムアウト（2h）で失敗。PR はすでに承認済みだがマージ未実施の状態で宙に浮いた。

### Run 4 で状態回復・マージ完了

Run 4 は新規 direct run として起動し、「PR #52 は承認済みだがマージされていない」と状況を確認してマージを実行・issue を done に更新。

### Run 5 はサインオフ対応

Run 5 の "PR確認OK" は already-done の状態で沈黙。

## 教訓

1. **PR承認後のマージ処理がタイムアウトすると PR が宙に浮く** — "PR確認OK" 直後のタイムアウトはマージ未完という中途半端な状態を生む。マージ処理は軽量なので retry が早期に起動するよう工夫できると良い。
2. **状態の確認と回復は新規 direct run が有効** — Run 4 は "どこまで進んでいるか" を確認しながらマージまで完了できた。PR URL が result_output に記録されていれば状態確認が容易。
3. **機械的リファクタリングは issue description に `biome --unsafe` 可否を明記すると良い** — 今回は description に `大半は biome --unsafe で自動修正可能` と記載されており、Run 1 でスムーズに作業が進んだ。
