```json
{
  "identifier": "LIN-192",
  "title": "noNonNullAssertion (!) の除去",
  "status": "done",
  "run_count": 4,
  "total_tokens": 8180895,
  "has_real_failures": false,
  "failure_cause": null,
  "rerun_causes": ["spec-feature-addition"],
  "patterns": ["spec-feature-addition"],
  "countermeasure": "none",
  "written_at": "2026-05-16"
}
```

# LIN-192: noNonNullAssertion (!) の除去

## 実行履歴サマリー

| Run | Status | トリガー |
|-----|--------|---------|
| 1 | completed | (初回 direct — トリガーなし) |
| 2 | completed | PR確認OK |
| 3 | completed | TypeScriptエラーを解消してからPR更新おねがいします |
| 4 | completed | PRがコンフリクトしたので修正してそのままPRをマージしてください |

## 観察

### 初回完了後に追加対応が2回発生

Run 1 で7ファイル13箇所の非nullアサーション (`!`) を `?.`・型ガード・const キャプチャに置き換え、PR #53 を作成。`tsc --noEmit` では "新規エラーなし（既存の DataRow.tsx TS6133 のみ）" と報告した。

### 「既存」とされたエラーの修正依頼

Run 2 の "PR確認OK" の後、ユーザーから Run 3 トリガーで DataRow.tsx の TS6133（未使用変数）の修正依頼が来た。初回実装で "既存エラー" として残した箇所が「既存ではない」と判断されたもので、仕様の解釈齟齬に相当する。

### コンフリクト解消（main の並行更新）

Run 4 は main への他PR（LIN-193・LIN-196など）のマージによるコンフリクト対応。2回 rebase が必要だった。

### failed なし

全4 runが completed。失敗なし。

## 教訓

1. **「既存エラー」の扱いは事前確認を** — 初回実装で既存エラーとして残した箇所が後から修正対象になった。issueの説明か実装前コメントで「既存エラーは対象外か？」を確認すると手戻りを防げる。
2. **並行PR環境ではコンフリクトが頻発する** — lint修正系の複数issueが同時進行すると同一ファイルを変更するケースが多く、コンフリクトはほぼ必然。rebase 前提で進めると良い。
