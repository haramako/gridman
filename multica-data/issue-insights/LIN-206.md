```json
{
  "identifier": "LIN-206",
  "title": "レポジトリテスト",
  "status": "done",
  "run_count": 3,
  "total_tokens": 84716,
  "has_real_failures": true,
  "failure_cause": "runtime_offline / queued_expired",
  "rerun_causes": ["duplicate-trigger"],
  "patterns": ["platform-artifact"],
  "countermeasure": "one-time",
  "written_at": "2026-06-05"
}
```

# LIN-206: レポジトリテスト

## 実行履歴サマリー

| Run | Status | トリガー |
|-----|--------|---------|
| 1 | failed | (初期トリガー) — runtime_offline |
| 2 | failed | (キュー) — queued_expired |
| 3 | completed | 再度実行して |

## 観察

最初の2回の実行が失敗。Run1は `runtime_offline`（ランタイムがオフライン）、Run2は `queued_expired`（キューで期限切れ）。Run3でユーザーが「再度実行して」とリクエストし、正常完了。レポジトリ概要をwiki参照して10行程度で説明するシンプルタスク。

## 教訓

1. **ランタイム環境の安定性** — 初期実行で `runtime_offline` になったケースは環境依存の問題。再実行で解決した。
2. **キュー待ちのタイムアウト** — `queued_expired` はリソース不足時の一時的現象。
