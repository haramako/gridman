```json
{
  "identifier": "LIN-174",
  "title": "【リファクタ】ダイアログ群の DRY 違反解消（共通 wrapper パターン導入）",
  "status": "done",
  "run_count": 1,
  "total_tokens": 4336120,
  "has_real_failures": false,
  "failure_cause": null,
  "rerun_causes": [],
  "patterns": [],
  "countermeasure": "one-time",
  "written_at": "2026-05-17"
}
```

# LIN-174: 【リファクタ】ダイアログ群の DRY 違反解消（共通 wrapper パターン導入）

## 実行履歴サマリー

| Run | Status | トリガー |
|-----|--------|---------|
| 1 | completed | (初回 direct — トリガーなし) |

## 観察

### 1回で完了した大規模リファクタ

4つのダイアログ（Filter, Lookup, SchemaEditor, PageTemplate）と Union ダイアログ計5ファイルを対象にした DRY 違反解消を、再実行なしで完了。以下を新規作成してリファクタした：

- `DialogShell.tsx` — モーダルオーバーレイ・ヘッダー（タイトル＋×ボタン）・Escape キー処理を共通化
- `DialogFooter.tsx` — 削除ボタン・キャンセル・保存の共通フッター
- `viewRowSource.ts` — `_source`/`_sources` 座標変換ロジックを `getEffectiveTableName`・`getRowOwnerTable` に集約
- `utils.ts` に `makeId()` を追加（3ダイアログで重複していた）

PR #39 を作成してレビュー待ちに。特筆すべき失敗・再実行はなし。

## 教訓

1. **リファクタは依存関係の整理が鍵** — description に「LIN-170（ColumnType 集約）完了後に取り組むと効果が高い」と明記されており、前提 issue の完了後に取り掛かったことでスムーズに進んだ。
2. **共通 wrapper パターンの抽出方針を description に書くと効果的** — 「何を共通化するか」（モーダル・フッター・座標変換・ID生成）が具体的に書かれており、1回の run で全件完了できた。
